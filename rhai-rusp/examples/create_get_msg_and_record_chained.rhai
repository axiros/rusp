print(rusp::get_builder()
    .with_params(["Device."])
    .with_max_depth(2)
    .build()
    .as_msg_builder()
    .with_msg_id("Foo")
    .as_no_session_record_builder()
    .with_version("1.3")
    .with_to_id("proto::to")
    .with_from_id("proto::from")
    .build())
